<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lst.comadep.Dao.TestDao">
    <insert id="inputMapDate" parameterType="map">
        insert into comadep.com_map (id,building_width,building_length,building_height)
        values (#{id},#{buildingWidth},#{buildingLength},#{buildingHeight})
    </insert>

</mapper>